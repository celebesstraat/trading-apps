# Trading Apps Monorepo - Environment Variables Template
# Copy this file to .env in the root directory and fill in your values

# This is a root-level template. Individual apps may have their own .env files.
# See each app's .env.example for app-specific configuration.

# Global configuration (if needed in future)
# Add monorepo-wide environment variables here
